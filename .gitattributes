# Normalize line endings on commit; check out native by default.
* text=auto

# Shell scripts must stay LF or they won't run on Unix.
*.sh text eol=lf

# Windows batch scripts stay CRLF.
*.bat text eol=crlf
